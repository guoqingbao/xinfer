name: release

on:
  workflow_dispatch:
    inputs:
      release_tag:
        description: "Existing release tag to rebuild, for example v0.13.8"
        required: true
        default: "v0.13.8"
  push:
    tags:
      - "v*.*.*"

permissions:
  contents: write
  id-token: write
  packages: write

env:
  CARGO_TERM_COLOR: always

jobs:
  # ---------------------------------------------------------------------------
  # Validate that all package versions match the tag
  # ---------------------------------------------------------------------------
  validate:
    name: validate-version
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}
      tag: ${{ steps.version.outputs.tag }}
    steps:
      - uses: actions/checkout@v4
      - name: Validate package versions
        id: version
        run: |
          cargo_version="$(sed -n 's/^version = "\(.*\)"/\1/p' Cargo.toml | head -n 1)"
          npm_asset="$(node -p "require('./npm/package.json').assetVersion || require('./npm/package.json').version")"
          if [ "$cargo_version" != "$npm_asset" ]; then
            echo "Cargo.toml version ($cargo_version) must match npm/package.json assetVersion ($npm_asset)" >&2
            exit 1
          fi
          expected_tag="v${cargo_version}"
          release_tag="${{ github.event_name == 'workflow_dispatch' && inputs.release_tag || github.ref_name }}"
          if [ "$release_tag" != "$expected_tag" ]; then
            echo "Tag $release_tag must match package version $expected_tag" >&2
            exit 1
          fi
          echo "version=${cargo_version}" >> "$GITHUB_OUTPUT"
          echo "tag=${release_tag}" >> "$GITHUB_OUTPUT"

  # ---------------------------------------------------------------------------
  # CUDA x64 container builds:
  # - SM70/75/80/89 use CUDA 12.6 for broad driver compatibility.
  # - Build in Ubuntu 20.04 so these artifacts run on Ubuntu 20.04-era hosts.
  # - GitHub-hosted runners need low build parallelism for flashinfer/cutlass.
  # ---------------------------------------------------------------------------
  cuda-x64:
    name: cuda-${{ matrix.sm_name }}
    runs-on: ubuntu-latest
    needs: validate
    strategy:
      fail-fast: false
      max-parallel: 1
      matrix:
        include:
          - sm: "70"
            sm_name: sm70
            cuda_version: "12.6.3"
            features: "cuda,nccl"
            python_features: "cuda,nccl,python"
          - sm: "75"
            sm_name: sm75
            cuda_version: "12.6.3"
            features: "cuda,nccl"
            python_features: "cuda,nccl,python"
          - sm: "80"
            sm_name: sm80
            cuda_version: "12.6.3"
            features: "cuda,nccl,flashinfer,cutlass"
            python_features: "cuda,nccl,flashinfer,cutlass,python"
          - sm: "89"
            sm_name: sm89
            cuda_version: "12.6.3"
            features: "cuda,nccl,flashinfer,cutlass"
            python_features: "cuda,nccl,flashinfer,cutlass,python"
    container:
      image: docker.io/nvidia/cuda:${{ matrix.cuda_version }}-cudnn-devel-ubuntu20.04
    env:
      CUDA_COMPUTE_CAP: ${{ matrix.sm }}
      DEBIAN_FRONTEND: noninteractive
      CARGO_BUILD_JOBS: "2"
      CUDAFORGE_THREADS: "2"
      RAYON_NUM_THREADS: "2"
    steps:
      - uses: actions/checkout@v4

      # Ubuntu 20.04 ships git 2.25.1, whose sparse-checkout chokes during
      # cudaforge's `git clone --filter/--sparse` of the flashinfer repo:
      #   fatal: cannot change to '<remote-url>': No such file or directory
      #   error: failed to initialize sparse-checkout
      # Only sm80/sm89 hit this (they enable the `flashinfer` feature, which
      # triggers cudaforge to clone flashinfer; sm70/sm75 use `cuda,nccl` only
      # and never invoke the clone). Pull a modern git from the git-core PPA.
      - name: Install system dependencies
        run: |
          apt-get update
          apt-get install -y --no-install-recommends --allow-change-held-packages \
            software-properties-common ca-certificates curl gnupg
          add-apt-repository -y ppa:git-core/ppa
          apt-get update
          apt-get install -y --no-install-recommends --allow-change-held-packages \
            libnccl-dev libnccl2 \
            curl git ca-certificates \
            clang libclang-dev \
            python3-pip patchelf dpkg-dev \
            perl make
          rm -rf /var/lib/apt/lists/*
          git --version

      - name: Install Rust
        run: |
          curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y
          echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"

      - name: Install maturin and cargo-deb
        run: |
          . "$HOME/.cargo/env"
          pip3 install --no-cache-dir maturin patchelf cffi
          cargo install cargo-deb

      - name: Build xinfer binary
        run: |
          . "$HOME/.cargo/env"
          cargo build --release --bin xinfer --features "${{ matrix.features }}"

      - name: Build Python wheel
        run: |
          . "$HOME/.cargo/env"
          ./build.sh --release --features "${{ matrix.python_features }}"

      - name: Bundle runtime libraries with binary
        run: bash .github/scripts/bundle-binary-libs.sh target/release/xinfer target/release/lib

      - name: Package deb
        run: |
          . "$HOME/.cargo/env"
          cargo deb --no-build --output "dist/xinfer-linux-x64-${{ matrix.sm_name }}.deb"

      - name: Package tar.gz
        run: |
          mkdir -p dist/xinfer
          cp target/release/xinfer dist/xinfer/xinfer
          cp -a target/release/lib dist/xinfer/lib
          tar -C dist/xinfer -czf "dist/xinfer-${{ needs.validate.outputs.version }}-linux-x64-${{ matrix.sm_name }}.tar.gz" xinfer lib
          rm -rf dist/xinfer

      - name: Collect and tag wheel with SM variant
        run: |
          mkdir -p dist/wheels
          for whl in target/wheels/*.whl; do
            base="$(basename "$whl")"
            tagged="$(echo "$base" | sed "s/-cp/-${{ matrix.sm }}-cp/")"
            cp "$whl" "dist/wheels/$tagged"
          done
          ls -la dist/wheels/

      - uses: actions/upload-artifact@v4
        with:
          name: xinfer-cuda-${{ matrix.sm_name }}
          path: |
            dist/*.tar.gz
            dist/*.deb
            dist/wheels/*.whl

  # ---------------------------------------------------------------------------
  # CUDA 13 x64 builds: SM90+ on self-hosted runner (CUDA 13.0 already installed)
  # Builds run sequentially (max-parallel: 1) switching CUDA_COMPUTE_CAP.
  # ---------------------------------------------------------------------------
  cuda:
    name: cuda-${{ matrix.sm_name }}
    runs-on: self-hosted
    needs: validate
    strategy:
      fail-fast: false
      max-parallel: 1
      matrix:
        include:
          - sm: "90"
            sm_name: sm90
            features: "cuda,nccl,flashinfer,cutlass"
            python_features: "cuda,nccl,flashinfer,cutlass,python"
          - sm: "100"
            sm_name: sm100
            features: "cuda,nccl,flashinfer,cutlass"
            python_features: "cuda,nccl,flashinfer,cutlass,python"
          - sm: "120"
            sm_name: sm120
            features: "cuda,nccl,flashinfer,cutlass"
            python_features: "cuda,nccl,flashinfer,cutlass,python"
    env:
      CUDA_COMPUTE_CAP: ${{ matrix.sm }}
      RAYON_NUM_THREADS: "128"
    steps:
      - uses: actions/checkout@v4

      - name: Clean previous build artifacts
        run: cargo clean 2>/dev/null || true

      - name: Ensure perl available for vendored OpenSSL build
        run: |
          if ! command -v perl >/dev/null 2>&1; then
            if command -v apt-get >/dev/null 2>&1; then
              sudo apt-get update && sudo apt-get install -y perl make
            else
              echo "ERROR: perl is required to build vendored OpenSSL." >&2
              exit 1
            fi
          fi
          command -v make >/dev/null 2>&1 || { echo "ERROR: make is required to build vendored OpenSSL." >&2; exit 1; }

      - name: Build xinfer binary
        run: cargo build --release --bin xinfer --features "${{ matrix.features }}"

      - name: Build Python wheel
        run: ./build.sh --release --features "${{ matrix.python_features }}"

      - name: Bundle runtime libraries with binary
        run: bash .github/scripts/bundle-binary-libs.sh target/release/xinfer target/release/lib

      - name: Package deb
        run: cargo deb --no-build --output "dist/xinfer-linux-x64-${{ matrix.sm_name }}.deb"

      - name: Package tar.gz
        run: |
          mkdir -p dist/xinfer
          cp target/release/xinfer dist/xinfer/xinfer
          cp -a target/release/lib dist/xinfer/lib
          tar -C dist/xinfer -czf "dist/xinfer-${{ needs.validate.outputs.version }}-linux-x64-${{ matrix.sm_name }}.tar.gz" xinfer lib
          rm -rf dist/xinfer

      - name: Collect and tag wheel with SM variant
        run: |
          mkdir -p dist/wheels
          for whl in target/wheels/*.whl; do
            base="$(basename "$whl")"
            tagged="$(echo "$base" | sed "s/-cp/-${{ matrix.sm }}-cp/")"
            cp "$whl" "dist/wheels/$tagged"
          done
          ls -la dist/wheels/

      - uses: actions/upload-artifact@v4
        with:
          name: xinfer-cuda-${{ matrix.sm_name }}
          path: |
            dist/*.tar.gz
            dist/*.deb
            dist/wheels/*.whl

  # ---------------------------------------------------------------------------
  # CUDA ARM build: SM121 (DGX Spark / GB10) — native aarch64 GitHub runner
  # Uses GitHub's ARM64 runner with CUDA 13 container.
  # Limited parallelism (2 threads) to fit runner resources.
  # ---------------------------------------------------------------------------
  cuda-sm121:
    name: cuda-sm121
    runs-on: ubuntu-24.04-arm
    needs: validate
    container:
      image: docker.io/nvidia/cuda:13.0.0-cudnn-devel-ubuntu24.04
    env:
      CUDA_COMPUTE_CAP: "121"
      DEBIAN_FRONTEND: noninteractive
      CUDAFORGE_THREADS: "2"
      RAYON_NUM_THREADS: "2"
    steps:
      - uses: actions/checkout@v4

      - name: Install system dependencies
        run: |
          apt-get update
          apt-get install -y --no-install-recommends --allow-change-held-packages \
            libnccl-dev libnccl2 \
            curl git ca-certificates \
            clang libclang-dev \
            python3-pip patchelf dpkg-dev \
            perl make
          rm -rf /var/lib/apt/lists/*

      - name: Install Rust
        run: |
          curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y
          echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"

      - name: Install maturin and cargo-deb
        run: |
          . "$HOME/.cargo/env"
          pip3 install --no-cache-dir --break-system-packages maturin patchelf cffi
          cargo install cargo-deb

      - name: Build xinfer binary
        run: |
          . "$HOME/.cargo/env"
          cargo build --release --bin xinfer --features "cuda,nccl,flashinfer,cutlass"

      - name: Build Python wheel
        run: |
          . "$HOME/.cargo/env"
          ./build.sh --release --features "cuda,nccl,flashinfer,cutlass,python"

      - name: Bundle runtime libraries with binary
        run: bash .github/scripts/bundle-binary-libs.sh target/release/xinfer target/release/lib

      - name: Package deb
        run: |
          . "$HOME/.cargo/env"
          cargo deb --no-build --output "dist/xinfer-linux-arm64-sm121.deb"

      - name: Package tar.gz
        run: |
          mkdir -p dist/xinfer
          cp target/release/xinfer dist/xinfer/xinfer
          cp -a target/release/lib dist/xinfer/lib
          tar -C dist/xinfer -czf "dist/xinfer-${{ needs.validate.outputs.version }}-linux-arm64-sm121.tar.gz" xinfer lib
          rm -rf dist/xinfer

      - name: Collect and tag wheel with SM variant
        run: |
          mkdir -p dist/wheels
          for whl in target/wheels/*.whl; do
            base="$(basename "$whl")"
            tagged="$(echo "$base" | sed "s/-cp/-121-cp/")"
            cp "$whl" "dist/wheels/$tagged"
          done
          ls -la dist/wheels/

      - uses: actions/upload-artifact@v4
        with:
          name: xinfer-cuda-sm121
          path: |
            dist/*.tar.gz
            dist/*.deb
            dist/wheels/*.whl

  # ---------------------------------------------------------------------------
  # Metal build (macOS) — GitHub-hosted runner
  # ---------------------------------------------------------------------------
  metal:
    name: metal-${{ matrix.npm_target }}
    runs-on: macos-latest
    needs: validate
    env:
      # Metal BF16 kernels require macOS 14+ / Metal 3.1. Lower deployment
      # targets compile successfully but omit __HAVE_BFLOAT__ kernel variants.
      MACOSX_DEPLOYMENT_TARGET: "14.0"
    strategy:
      fail-fast: false
      matrix:
        include:
          - rust_target: aarch64-apple-darwin
            npm_target: darwin-arm64
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.rust_target }}

      - name: Install maturin
        run: pip3 install --break-system-packages maturin cffi

      - name: Build xinfer binary
        run: cargo build --release --target ${{ matrix.rust_target }} --bin xinfer --features "metal"

      - name: Build Python wheel
        run: ./build.sh --release --features "metal,python"

      - name: Package tar.gz
        run: |
          mkdir -p "dist/xinfer"
          cp "target/${{ matrix.rust_target }}/release/xinfer" "dist/xinfer/xinfer"
          tar -C dist/xinfer -czf "dist/xinfer-${{ needs.validate.outputs.version }}-${{ matrix.npm_target }}-metal.tar.gz" xinfer
          rm -rf dist/xinfer

      - name: Collect wheel
        run: |
          mkdir -p dist/wheels
          cp target/wheels/*.whl dist/wheels/
          ls -la dist/wheels/

      - uses: actions/upload-artifact@v4
        with:
          name: xinfer-${{ matrix.npm_target }}-metal
          path: |
            dist/*.tar.gz
            dist/wheels/*.whl

  # ---------------------------------------------------------------------------
  # Checksums + GitHub Release
  # ---------------------------------------------------------------------------
  checksums:
    name: checksums
    runs-on: ubuntu-latest
    needs: [validate, cuda-x64, cuda, cuda-sm121, metal]
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true

      - name: Generate SHA256SUMS
        run: |
          cd dist
          find . -type f \( -name "*.tar.gz" -o -name "*.deb" -o -name "*.whl" \) \
            -exec sha256sum {} + > SHA256SUMS
          cat SHA256SUMS

      - uses: actions/upload-artifact@v4
        with:
          name: xinfer-release-artifacts
          path: dist/*

      - name: Upload artifacts to release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ needs.validate.outputs.tag }}
          name: xInfer v${{ needs.validate.outputs.version }}
          generate_release_notes: true
          overwrite_files: true
          files: |
            dist/*.tar.gz
            dist/*.deb
            dist/wheels/*.whl
            dist/SHA256SUMS

  # ---------------------------------------------------------------------------
  # Publish wheel index to GitHub Pages (PEP 503 simple repository)
  # Each CUDA arch gets its own index URL:
  #   pip install xinfer --index-url https://guoqingbao.github.io/xinfer/sm80/  (default)
  #   pip install xinfer --index-url https://guoqingbao.github.io/xinfer/sm70/  (V100)
  #   pip install xinfer --index-url https://guoqingbao.github.io/xinfer/sm90/  (Hopper)
  #   pip install xinfer --index-url https://guoqingbao.github.io/xinfer/sm100/ (B100/B200)
  #   pip install xinfer --index-url https://guoqingbao.github.io/xinfer/sm120/ (RTX 5090/RTX PRO 6000)
  #   pip install xinfer --index-url https://guoqingbao.github.io/xinfer/sm121/ (Spark/GB10 ARM)
  #   pip install xinfer --index-url https://guoqingbao.github.io/xinfer/metal/ (macOS)
  # The root index (no arch) defaults to SM80.
  # ---------------------------------------------------------------------------
  publish-index:
    name: publish-index
    runs-on: ubuntu-latest
    needs: [validate, checksums]
    permissions:
      contents: read
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    concurrency:
      group: pages
      cancel-in-progress: false
    steps:
      - uses: actions/checkout@v4
        with:
          sparse-checkout: paged

      - uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true

      - name: Build PEP 503 index
        env:
          VERSION: ${{ needs.validate.outputs.version }}
          REPO: ${{ github.repository }}
          TAG: ${{ needs.validate.outputs.tag }}
        run: |
          RELEASE_BASE="https://github.com/${REPO}/releases/download/${TAG}"

          generate_index() {
            local dir="$1" release_name="$2" std_name="$3" sha="$4"
            local href="${RELEASE_BASE}/${release_name}"
            if [ -n "$sha" ]; then href="${href}#sha256=${sha}"; fi
            mkdir -p "site/${dir}/xinfer"
            {
              echo '<!DOCTYPE html><html><body>'
              echo "<a href=\"${href}\">${std_name}</a><br/>"
              echo '</body></html>'
            } > "site/${dir}/xinfer/index.html"
            echo "<!DOCTYPE html><html><body><a href=\"xinfer/\">xinfer</a></body></html>" \
              > "site/${dir}/index.html"
          }

          for whl in dist/wheels/*.whl; do
            [ -f "$whl" ] || continue
            orig_name="$(basename "$whl")"
            sha="$(sha256sum "$whl" | cut -d' ' -f1)"

            # Detect variant tag from filename: SM number or "metal"
            tag="$(echo "$orig_name" | sed -n 's/.*-\([0-9]\{2,3\}\)-cp.*/\1/p')"
            if [ -n "$tag" ]; then
              std_name="$(echo "$orig_name" | sed "s/-${tag}-cp/-cp/")"
              generate_index "sm${tag}" "$orig_name" "$std_name" "$sha"
              if [ "$tag" = "80" ]; then
                mkdir -p "site/xinfer"
                cp "site/sm${tag}/xinfer/index.html" "site/xinfer/index.html"
              fi
            elif echo "$orig_name" | grep -qE '\-(metal\-cp|macosx_)'; then
              std_name="$(echo "$orig_name" | sed 's/-metal-cp/-cp/')"
              # pip parses wheel tags from the URL filename; use PEP 427 name on release
              generate_index "metal" "$std_name" "$std_name" "$sha"
            fi
          done

          cp -r paged/* site/
          sed -i "s|XINFER_VER = '[^']*'|XINFER_VER = '${VERSION}'|" site/assets/js/main.js
          sed -i "s|XINFER_VER='[^']*'|XINFER_VER='${VERSION}'|" site/mobile/index.html

          # Auto-detect install script
          cat > site/install.sh << 'SCRIPT'
          #!/usr/bin/env bash
          set -euo pipefail

          VERSION="__VERSION__"
          INDEX_BASE="https://guoqingbao.github.io/xinfer"
          RELEASE_BASE="https://github.com/__REPO__/releases/download/v${VERSION}"

          detect_platform() {
            if [ "$(uname -s)" = "Darwin" ]; then
              echo "metal"
              return
            fi
            if ! command -v nvidia-smi &>/dev/null; then
              echo "ERROR: No NVIDIA GPU detected and no platform specified." >&2
              echo "  Set XINFER_PLATFORM=sm80 (or sm70/sm75/sm89/sm90/sm100/sm120/sm121) and re-run." >&2
              exit 1
            fi
            local cc
            cc="$(nvidia-smi --query-gpu=compute_cap --format=csv,noheader,nounits | head -1 | tr -d '[:space:]' | tr -d '.')"
            case "$cc" in
              70|72) echo "sm70" ;;
              75)    echo "sm75" ;;
              80|86) echo "sm80" ;;
              89)    echo "sm89" ;;
              90|90a) echo "sm90" ;;
              100|100a) echo "sm100" ;;
              120|120a) echo "sm120" ;;
              121|121a) echo "sm121" ;;
              *) echo "sm80" ;;
            esac
          }

          cuda_build_for_platform() {
            case "$1" in
              sm70|sm75|sm80|sm89) echo "12.6" ;;
              sm90|sm100|sm120|sm121) echo "13.0" ;;
              metal)               echo "none" ;;
              *)                   echo "12.6" ;;
            esac
          }

          min_driver_for_cuda() {
            case "$1" in
              13.*) echo "580.65.06" ;;
              12.*) echo "525.60.13" ;;
              *)    echo "525.60.13" ;;
            esac
          }

          version_ge() {
            [ "$1" = "$2" ] && return 0
            [ "$(printf '%s\n%s\n' "$2" "$1" | sort -V | head -n1)" = "$2" ]
          }

          detect_driver_version() {
            if ! command -v nvidia-smi &>/dev/null; then
              return 1
            fi
            nvidia-smi --query-gpu=driver_version --format=csv,noheader 2>/dev/null \
              | head -1 | tr -d '[:space:]'
          }

          check_driver_compat() {
            local platform="$1"
            local cuda_build min_driver driver
            cuda_build="$(cuda_build_for_platform "$platform")"
            if [ "$cuda_build" = "none" ]; then
              return 0
            fi
            if [ "${XINFER_SKIP_DRIVER_CHECK:-}" = "1" ]; then
              echo "Skipping NVIDIA driver compatibility check because XINFER_SKIP_DRIVER_CHECK=1."
              return 0
            fi
            min_driver="$(min_driver_for_cuda "$cuda_build")"
            driver="$(detect_driver_version || true)"
            if [ -z "$driver" ]; then
              echo "ERROR: Could not detect NVIDIA driver with nvidia-smi." >&2
              echo "  ${platform} package is built with CUDA ${cuda_build} and requires driver >= ${min_driver}." >&2
              echo "  Set XINFER_SKIP_DRIVER_CHECK=1 only if you know the target runtime driver is compatible." >&2
              exit 1
            fi
            if ! version_ge "$driver" "$min_driver"; then
              echo "ERROR: NVIDIA driver ${driver} is too old for ${platform} package." >&2
              echo "  ${platform} package is built with CUDA ${cuda_build} and requires driver >= ${min_driver}." >&2
              echo "  Upgrade the NVIDIA driver or install a package built for an older CUDA family." >&2
              exit 1
            fi
            echo "NVIDIA driver ${driver} is compatible with ${platform} CUDA ${cuda_build} package."
          }

          PLATFORM="${XINFER_PLATFORM:-$(detect_platform)}"
          echo "Detected platform: ${PLATFORM}"
          check_driver_compat "$PLATFORM"
          echo ""

          need_sudo() {
            [ "$(id -u)" -ne 0 ]
          }

          SUDO=""
          if need_sudo; then
            if command -v sudo &>/dev/null; then
              SUDO="sudo"
            fi
          fi

          print_binary_hints() {
            echo ""
            echo "============================================"
            echo " xInfer binary installed"
            echo "============================================"
            echo ""
            echo " HuggingFace model:"
            echo "   xinfer --m Qwen/Qwen3-8B --ui-server"
            echo ""
            echo " Local model path:"
            echo "   xinfer --w /path/to/model --ui-server"
            echo ""
            echo " API server (no UI):"
            echo "   xinfer --m Qwen/Qwen3-8B --server"
            echo "============================================"
          }

          install_deb() {
            local variant="$1"
            local cpu_arch="x64"
            if [ "$variant" = "sm121" ]; then cpu_arch="arm64"; fi
            local deb="xinfer-linux-${cpu_arch}-${variant}.deb"
            local url="${RELEASE_BASE}/${deb}"
            local tmp
            tmp="$(mktemp -d)"
            echo "Downloading ${deb}..."
            if ! curl -fSL -o "${tmp}/${deb}" "$url"; then
              echo "ERROR: Failed to download ${url}" >&2
              rm -rf "$tmp"
              return 1
            fi
            echo "Installing ${deb}..."
            if ! ${SUDO} dpkg -i "${tmp}/${deb}"; then
              echo ""
              echo "ERROR: dpkg -i failed. You may need root privileges:" >&2
              echo "  sudo dpkg -i ${tmp}/${deb}" >&2
              echo "  Or re-run with: curl -sSL ${INDEX_BASE}/install.sh | sudo bash" >&2
              return 1
            fi
            rm -rf "$tmp"
            print_binary_hints
          }

          install_tarball() {
            local tarball="$1"
            local url="${RELEASE_BASE}/${tarball}"
            local install_dir="/usr/local/bin"
            local lib_dir="/usr/local/lib/xinfer"
            local tmp
            tmp="$(mktemp -d)"
            echo "Downloading ${tarball}..."
            if ! curl -fSL -o "${tmp}/${tarball}" "$url"; then
              echo "ERROR: Failed to download ${url}" >&2
              rm -rf "$tmp"
              return 1
            fi
            echo "Extracting..."
            tar -xzf "${tmp}/${tarball}" -C "${tmp}"
            echo "Installing xinfer to ${install_dir}..."
            if ! ${SUDO} install -m 755 "${tmp}/xinfer" "${install_dir}/xinfer"; then
              echo ""
              echo "ERROR: Could not install to ${install_dir}." >&2
              echo "  Try: sudo install -m 755 ${tmp}/xinfer ${install_dir}/xinfer" >&2
              echo "  Or re-run with: curl -sSL ${INDEX_BASE}/install.sh | sudo bash" >&2
              return 1
            fi
            if [ -d "${tmp}/lib" ]; then
              ${SUDO} mkdir -p "${lib_dir}"
              ${SUDO} cp -a "${tmp}/lib/"* "${lib_dir}/"
            fi
            rm -rf "$tmp"
            print_binary_hints
          }

          install_pip() {
            local platform="$1"
            local PIP=""
            if command -v pip3 &>/dev/null; then
              PIP=pip3
            elif command -v pip &>/dev/null; then
              PIP=pip
            else
              echo "ERROR: pip not found. Install Python 3 first." >&2
              return 1
            fi
            echo "Installing: ${PIP} install xinfer --index-url ${INDEX_BASE}/${platform}/"
            if ! ${PIP} install xinfer --index-url "${INDEX_BASE}/${platform}/"; then
              echo ""
              echo "ERROR: pip install failed." >&2
              echo "  Try: ${SUDO:+sudo }${PIP} install xinfer --index-url ${INDEX_BASE}/${platform}/" >&2
              return 1
            fi
            echo ""
            echo "============================================"
            echo " xInfer installed via Python package"
            echo "============================================"
            echo ""
            echo " HuggingFace model:"
            echo "   python3 -m xinfer.server --m Qwen/Qwen3-8B --ui-server"
            echo ""
            echo " Local model path:"
            echo "   python3 -m xinfer.server --w /path/to/model --ui-server"
            echo ""
            echo " API server (no UI):"
            echo "   python3 -m xinfer.server --m Qwen/Qwen3-8B --server"
            echo "============================================"
          }

          if [ "$PLATFORM" = "metal" ]; then
            TARBALL="xinfer-${VERSION}-darwin-arm64-metal.tar.gz"
            echo "Install options:"
            echo "  1) binary  — installs 'xinfer' to /usr/local/bin"
            echo "  2) pip     — installs 'python3 -m xinfer.server'"
            echo ""
          elif [ "$PLATFORM" = "sm121" ]; then
            TARBALL="xinfer-${VERSION}-linux-arm64-sm121.tar.gz"
            echo "Install options:"
            echo "  1) deb package  — installs 'xinfer' system-wide (Linux)"
            echo "  2) pip package  — installs 'python3 -m xinfer.server'"
            echo "  3) binary       — installs 'xinfer' to /usr/local/bin"
            echo ""
          else
            TARBALL="xinfer-${VERSION}-linux-x64-${PLATFORM}.tar.gz"
            echo "Install options:"
            echo "  1) deb package  — installs 'xinfer' system-wide (Linux)"
            echo "  2) pip package  — installs 'python3 -m xinfer.server'"
            echo "  3) binary       — installs 'xinfer' to /usr/local/bin"
            echo ""
          fi

          MODE="${XINFER_MODE:-}"
          if [ -z "$MODE" ]; then
            if [ -e /dev/tty ]; then
              printf "Choose [1/2%s] (default: 1): " "$([ "$PLATFORM" != "metal" ] && echo "/3")"
              read -r MODE < /dev/tty
              MODE="${MODE:-1}"
            else
              MODE="1"
            fi
          fi

          if [ "$PLATFORM" = "metal" ]; then
            case "$MODE" in
              1|binary)  install_tarball "$TARBALL" ;;
              2|pip)     install_pip "$PLATFORM" ;;
              *)         echo "Invalid choice: $MODE"; exit 1 ;;
            esac
          else
            case "$MODE" in
              1|deb)     install_deb "$PLATFORM" ;;
              2|pip)     install_pip "$PLATFORM" ;;
              3|binary)  install_tarball "$TARBALL" ;;
              *)         echo "Invalid choice: $MODE"; exit 1 ;;
            esac
          fi
          SCRIPT

          # Substitute version and repo into the script
          sed -i "s|__VERSION__|${VERSION}|g" site/install.sh
          sed -i "s|__REPO__|${REPO}|g" site/install.sh
          sed -i 's/^          //' site/install.sh
          chmod +x site/install.sh

          echo "=== Generated indexes ==="
          for idx in site/*/xinfer/index.html site/xinfer/index.html; do
            [ -f "$idx" ] && echo "--- $idx ---" && cat "$idx"
          done

      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: site
      - uses: actions/deploy-pages@v4
        id: deploy

  # ---------------------------------------------------------------------------
  # Publish npm package
  # ---------------------------------------------------------------------------
  publish-npm:
    name: publish-npm
    runs-on: ubuntu-latest
    needs: [validate, checksums]
    environment: xInfer
    env:
      NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          registry-url: "https://registry.npmjs.org"
      - name: Check npm package version
        id: npm_version
        run: |
          VERSION="${{ needs.validate.outputs.version }}"
          PACKAGE_NAME="$(node -p "require('./npm/package.json').name")"
          LATEST_VERSION="$(npm view "$PACKAGE_NAME" version 2>/dev/null || true)"
          if npm view "${PACKAGE_NAME}@${VERSION}" version >/dev/null 2>&1; then
            echo "${PACKAGE_NAME}@${VERSION} already exists on npm; skipping publish."
            echo "exists=true" >> "$GITHUB_OUTPUT"
          else
            echo "${PACKAGE_NAME}@${VERSION} does not exist on npm; publishing is allowed."
            echo "exists=false" >> "$GITHUB_OUTPUT"
          fi
          echo "latest=${LATEST_VERSION:-unknown}" >> "$GITHUB_OUTPUT"
      - name: Skip when NPM_TOKEN is not configured
        if: env.NODE_AUTH_TOKEN == ''
        run: echo "NPM_TOKEN is not configured; skipping npm publish."
      - name: Skip when npm version already exists
        if: env.NODE_AUTH_TOKEN != '' && steps.npm_version.outputs.exists == 'true'
        run: echo "npm latest is ${{ steps.npm_version.outputs.latest }}; version ${{ needs.validate.outputs.version }} already exists."
      - name: Sync npm package version with release tag
        if: env.NODE_AUTH_TOKEN != '' && steps.npm_version.outputs.exists != 'true'
        run: |
          VERSION="${{ needs.validate.outputs.version }}"
          cd npm
          node -e "
            const pkg = require('./package.json');
            pkg.version = '${VERSION}';
            pkg.assetVersion = '${VERSION}';
            require('fs').writeFileSync('package.json', JSON.stringify(pkg, null, 2) + '\n');
          "
      - name: Publish npm package
        if: env.NODE_AUTH_TOKEN != '' && steps.npm_version.outputs.exists != 'true'
        run: npm publish ./npm --access public --provenance
